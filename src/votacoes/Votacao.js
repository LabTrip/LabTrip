
export default class Votacao{
    constructor(roteiroAtividadeId, usuarioId, gostou){
      this.roteiroAtividadeId = roteiroAtividadeId;
      this.usuarioId = usuarioId;
      this.gostou = gostou;
    }
  }