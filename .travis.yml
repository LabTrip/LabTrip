dist: xenial

language: node_js

node_js:
- node

dotnet: 2.1.502
script:
  # other script steps might be done before running the actual analysis
  - dotnet restore
  - dotnet test
  - msbuild /p:Configuration=Release solution-name.sl

cache: npm
addons:
  sonarcloud:
    organization: "labtrip" # the key of the org you chose at step #3
    token: ${SONAR_TOKEN} # encrypted value of your token
script:
  # other script steps might be done before running the actual analysis
  - sonar-scanner -X